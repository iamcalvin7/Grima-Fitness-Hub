Replit authentication is still looping, so I cannot send the approved build directly. Paste this into the Replit Agent.

APPROVED RED BUILD — Role-Based Permissions Gate 2

Gate 2 is approved as one bounded development-only execution envelope.

Proceed through all stages without requesting approval between them when the preceding checks pass. Stop only when a defined stop condition occurs.

Do not update documentation. ChatGPT will update it once the complete gate reaches 100%.

After each meaningful update, include:

* Plain-English impact
* GATE 2 TASK COMPLETION: X/100 — X%
* ROLE-BASED PERMISSIONS FEATURE SCORE: 71/100 — awaiting final AI Director rescore

Intended impact

Client

Every new upload path is generated by the server and tied to the authenticated Client. A Client cannot request another user’s folder or use path traversal.

Marcus

Marcus receives:

* A safe, repeatable provisioning tool
* An immutable audit trail for sensitive actions
* Protected access to audit records

No real user may be promoted during this build.

Approved scope

1. Inspect existing schema, migration, transaction, audit, privileged-route, storage and scripting conventions.
2. Add an append-only audit-log data model and development migration.
3. Apply and verify the additive migration in the confirmed development workspace.
4. Add a central audit-writing helper.
5. Audit existing sensitive Content and Proposal mutations.
6. Add a Marcus-only, tenant-scoped audit-read API.
7. Build and test a safe Marcus provisioning tool without executing it against a real user.
8. Bind upload object paths to the authenticated user.
9. Run complete verification and return one consolidated report.

Explicit exclusions

Do not:

* Access or modify production
* Publish or deploy
* Promote a real user
* Change authentication providers
* Change session architecture
* Add new product roles
* Remove the legacy trainer enum value
* Add audit-log editing or deletion
* Build an audit-log frontend
* Change unrelated features
* Update documentation
* Add production dependencies unless an existing project capability cannot safely perform the work

Stage 0 — Inspection and preflight — 15 points

Before editing, inspect:

* User, tenant and role schemas
* Existing migration conventions and migration history
* Database transaction patterns
* Capability middleware and audit:read
* Content mutation routes
* Proposal mutation/decision routes
* Object-storage signing route and helpers
* Existing scripts and package scripts
* Test infrastructure

Confirm positively that this is the development workspace using Replit deployment/development indicators.

Return:

* Exact proposed files
* Existing sensitive mutation inventory
* Existing upload request and generated-path behaviour
* Proposed additive audit schema
* Migration order
* Test plan
* Any compatibility risks

Stop if:

* The environment is deployed or cannot be confirmed as development
* The migration must be destructive
* An incompatible audit table already exists
* Existing route behaviour cannot be preserved
* Production credentials or real-user mutation would be required

Stage 1 — Audit infrastructure — 25 points

Create an additive audit-log schema following existing database conventions.

Minimum required information:

* Unique audit record ID
* Tenant/context ID where applicable
* Actor type
* Authenticated actor user ID when available
* Action identifier
* Target type
* Target identifier
* Sanitised metadata
* Timestamp

Requirements:

* Append-only
* No public create, update or delete API
* Appropriate indexes for tenant, actor, target, action and date queries
* No secrets, tokens, signed URLs or unnecessary personal data in metadata
* Missing audit context fails safely
* Audit records use server-derived identity, never caller-supplied actor identity

Create the migration, apply it only to the confirmed development database and physically verify:

* Table
* Columns and types
* Nullability
* Foreign keys where appropriate
* Indexes
* Migration history

Add a central audit-writing helper with focused tests.

For sensitive database mutations, the business mutation and audit write should be atomic where the existing database framework supports transactions. If safe atomicity cannot be implemented using existing conventions, stop before fan-out and report the blocker.

Stage 2 — Sensitive-action audit coverage — 20 points

Use one existing privileged mutation as the canary.

Recommended canary: one Content create, update or delete operation.

Prove:

* Marcus action succeeds and creates exactly one audit record
* Client remains denied and creates no audit record
* Legacy trainer remains denied and creates no audit record
* Failed validation creates no success audit record
* Audit failure prevents an unaudited sensitive mutation when atomicity is required
* Actor, action, tenant and target are server-derived and correct
* Metadata contains no secret or signed URL

After the canary passes, conditionally migrate the remaining matching sensitive mutations:

* Content create
* Content update
* Content delete
* Proposal creation or administrative mutation where applicable
* Proposal decision mutation

Do not audit read-only requests.

Return an exact action-to-audit-event table.

Add a paginated, tenant-scoped audit-read API protected by requireCapability("audit:read").

Prove:

* Marcus/admin may read the tenant’s audit records
* Client is denied
* Legacy trainer is denied
* Cross-tenant records are excluded
* Pagination has safe limits
* No audit update/delete endpoint exists

Stage 3 — Safe Marcus provisioning tool — 20 points

Build a development/operations CLI using existing project conventions.

Requirements:

* Dry-run by default
* Requires an exact existing user identifier
* Requires an explicit apply flag
* Requires confirmation that matches the target identifier
* Shows only minimal non-sensitive target information
* Refuses missing or ambiguous targets
* Refuses to create a second different Marcus/admin account
* Is idempotent when the target is already admin
* Never accepts a password or secret through command arguments
* Updates the role and writes the provisioning audit event in one transaction
* Returns a clear success or refusal result
* Does not expose a public HTTP provisioning endpoint

Add unit tests for:

* Dry run
* Missing target
* Ambiguous target
* Confirmation mismatch
* Existing different admin
* Already-admin idempotency
* Successful transactional promotion using mocks or isolated fixtures
* Audit failure rollback
* No sensitive output

Do not execute the apply path against any real user.

Stage 4 — User-scoped upload paths — 20 points

Change the upload-signing path so the server owns the full object key.

Requirements:

* Use the authenticated user ID
* Include tenant context if consistent with existing storage architecture
* Generate a unique server-controlled object identifier
* Accept only the minimum safe filename/extension information needed
* Reject or neutralise:
    * ..
    * Forward slashes
    * Backslashes
    * Absolute paths
    * Encoded traversal
    * Empty names
    * Invalid extensions or content types under existing upload rules
* Never accept a caller-supplied user ID, tenant ID, folder or full object key
* Preserve existing successful uploads and response contracts where safely possible
* Do not expose storage credentials

Add tests proving:

* Client A’s key is scoped to Client A
* Client B receives a different prefix
* A caller cannot request Client B’s path
* Traversal inputs cannot escape the authenticated prefix
* Missing authentication is denied
* Repeated filenames generate unique keys
* Existing allowed content types still work
* Existing invalid content types remain denied

Stage 5 — Full verification — 20 points

Run:

* All API tests
* All Client tests
* All-package typecheck
* API production build
* Client production build with safe inline PORT and BASE_PATH
* Migration/schema verification
* Working-tree scope review
* Lockfile and dependency review
* Environment-change check
* Secret and credential check
* Temporary-script and debug-output check

Baseline before Gate 2:

* 119 tests passing
* 19 tests skipped
* Zero failures
* Gate 1B complete
* Role-Based Permissions score 71/100

Do not convert blocked tests into passing tests by weakening assertions.

Stop conditions

Stop immediately if:

* Production or a deployed application environment is detected
* A destructive migration is required
* A real user would need to be promoted
* Audit and sensitive mutation cannot be made safely atomic
* Storage scoping would break an unknown existing consumer
* A new production dependency is required
* Tests, typechecks or builds regress
* Scope expands beyond Gate 2
* Secrets or personal data would be exposed

Completion rubric

* Inspection and development preflight: 15/15
* Audit schema, migration and helper: 25/25
* Sensitive-action audit coverage and read API: 20/20
* Safe Marcus provisioning tool: 20/20
* User-scoped upload paths: 20/20
* Full verification is required across the entire rubric; any failed mandatory check prevents 100%.

Final report

Return one consolidated report containing:

1. Plain-English Client impact
2. Plain-English Marcus impact
3. Environment confirmation
4. Files changed
5. Migration and physical schema evidence
6. Audit schema and helper behaviour
7. Sensitive action-to-event table
8. Audit-read API enforcement
9. Provisioning-tool safety and tests
10. Upload-key format and attack-case tests
11. Complete test totals
12. Typecheck and production-build results
13. Dependency, environment and working-tree hygiene
14. Remaining blockers
15. Stop conditions encountered

End with:

GATE 2 TASK COMPLETION: X/100 — X%

ROLE-BASED PERMISSIONS FEATURE SCORE: 71/100 — awaiting final AI Director rescore

Do not publish, promote a user, begin another feature, or update documentation.

GATE 2 TASK COMPLETION: 0/100 — 0%